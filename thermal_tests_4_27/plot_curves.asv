clear;
load('temperature_data_4_27');

external=temps(1:600,1);
long_axis=temps(1:600,2);
short_axis=temps(1:600,3);
time=transpose(1:length(external));

hold on;
plot(time,external);
plot(time,long_axis);
plot(time,short_axis);

legend('e